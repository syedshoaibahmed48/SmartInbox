services:
  redis:
    image: redis:7
    container_name: redis_server
    ports:
      - "6379:6379"
    restart: unless-stopped

  llm:
    build: ./llm
    container_name: llm_server
    ports:
      - "8080:8080"
    restart: unless-stopped